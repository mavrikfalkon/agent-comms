# Normalize line endings to LF for everyone, regardless of core.autocrlf.
# Without this, a Windows checkout gets CRLF working-tree files even though
# the repo stores LF, and `eslint .` (prettier) fails on files nobody
# actually edited — this is what broke the pre-push hook.
* text=auto eol=lf
